{
  "target": "isFraud",
  "drop_cols_baseline": ["TransactionID", "isFraud"],
  "drop_cols_engineered": ["TransactionID", "isFraud", "uid"],
  "train_fraction": 0.8,
  "decision_threshold": 0.5,
  "random_state": 42,
  "lightgbm": {
    "objective": "binary",
    "n_estimators": 500,
    "learning_rate": 0.05,
    "num_leaves": 64,
    "subsample": 0.8,
    "colsample_bytree": 0.8,
    "early_stopping_rounds": 50,
    "eval_metric": "average_precision",
    "n_jobs": -1,
    "verbose": -1
  }
}
