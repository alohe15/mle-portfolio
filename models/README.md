# Models

Trained model artifacts for the IEEE fraud detection project (`project-1-detect-fraud`).

Save serialized models, checkpoints, and related metadata here. Large binary files are gitignored; commit only lightweight artifacts (configs, metrics, small exports) when needed.
